<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.etherproject">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EtherProject">
        <activity android:name=".testing"
            android:label="SEND ETHERS AND TOKENS"
            android:parentActivityName=".Dashboard">
        </activity>
        <activity
            android:name=".importprivatekey"
            android:label="SIGN-IN"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".wallet"
            android:label="CREATE WALLET"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Settings"
            android:label="SETTINGS"
            android:parentActivityName=".Dashboard" />
        <activity
            android:name=".Dashboard"
            android:label="DASHBOARD" />
        <activity
            android:name=".MainActivity"
            android:label="ETHEREUM PROJECT"
            android:theme="@style/Theme.EtherProject.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>