<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EtherProject" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="customToast">
        <item name="stTextBold">true</item>
        <item name="stTextColor">#FFFFFF</item>
        <item name="stTextSize">14sp</item>
        <item name="stColorBackground">@color/cardview_dark_background</item>
        <item name="stSolidBackground">true</item>
        <item name="stIconStart">@mipmap/ic_launcher_tick</item>
        <item name="strokeColor">#000</item>
        <item name="strokeWidth">10dp</item>
        <item name="stLength">LONG</item>
        <item name="stGravity">center</item>
        <item name="cornerRadius">10dp</item>
    </style>

    <style name="mistakeToast">
        <item name="stTextBold">true</item>
        <item name="stTextColor">#FFFFFF</item>
        <item name="stTextSize">14sp</item>
        <item name="stColorBackground">@color/material_on_primary_emphasis_medium</item>
        <item name="stSolidBackground">true</item>
        <item name="stIconStart">@mipmap/ic_launcher_mistake</item>
        <item name="strokeColor">#000</item>
        <item name="strokeWidth">10dp</item>
        <item name="stLength">LONG</item>
        <item name="stGravity">center</item>
        <item name="cornerRadius">10dp</item>
    </style>

    <style name="Theme.EtherProject.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.EtherProject.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.EtherProject.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>